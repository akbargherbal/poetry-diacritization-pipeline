[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: tests that exercise many iterations or larger fixtures
    integration: tests that make a real call to the DeepSeek API (require DEEPSEEK_API_KEY; skipped by default)
addopts = -ra --strict-markers
